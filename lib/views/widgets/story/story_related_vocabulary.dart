import 'package:flutter/material.dart';

class StoryRelatedVocabulary extends StatelessWidget {
  const StoryRelatedVocabulary({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
