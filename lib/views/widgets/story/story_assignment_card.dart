import 'package:flutter/material.dart';

class StoryAssignmentCard extends StatelessWidget {
  const StoryAssignmentCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
