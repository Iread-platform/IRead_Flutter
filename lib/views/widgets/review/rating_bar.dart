import 'package:flutter/material.dart';

class RatingBar extends StatelessWidget {
  final double _rating;

  const RatingBar({double rating, Key key})
      : _rating = rating,
        super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
